import { createStyles, makeStyles } from '@material-ui/core';

export const useStyles = makeStyles(() =>
  createStyles({
    status: {
      marginTop: '45px',
      borderRadius: '8px',
      overflow: 'hidden'
    }
  })
);
