import { createStyles, makeStyles } from '@material-ui/core';

export const useStyles = makeStyles(() =>
  createStyles({
    noItems: {
      fontSize: '24px',
      fontWeight: 700,
      margin: 'auto'
    }
  })
);
