import { applicationPath } from 'src/constants';

export const background = `url(${applicationPath}/assets/img/backgroung.png) center/100%  no-repeat`;
export const logoBackground = `url(${applicationPath}/assets/img/logo.png) 100%  no-repeat`;
