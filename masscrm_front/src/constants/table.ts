import { IStatuses } from 'src/interfaces';

export const tooltipMessage =
  'The password change link can only be sent to a user who is not registered by corporate e-mail';

export const STATUSES: IStatuses = {
  Active: 1,
  Inactive: 0
};
