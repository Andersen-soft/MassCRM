import { makeStyles, createStyles } from '@material-ui/core';

export const useStyles = makeStyles(() =>
  createStyles({
    listTDLink: {
      display: 'block',
      padding: '3px 0',
      color: '#13639d',
      fontSize: '1rem'
    }
  })
);
